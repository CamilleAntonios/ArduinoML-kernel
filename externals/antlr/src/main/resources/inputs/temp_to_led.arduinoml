application tempToLed

sensor tempSensor : 1 analogical
actuator led : 10 analogical
sensor button : 9 digital

INIT transmit {
    led <= tempSensor
    if(button == HIGH) => halt
}

halt {
    led <= 0
    if(button == LOW) => transmit
}
